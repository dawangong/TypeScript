const isDone: boolean = false;

const createdByBoolean: boolean = Boolean(1);

const decLiteral: number = 1;

const myAge: number = 25;
const sentence: string = `Hello, my name is ${myAge}.`;

const unusable: void = undefined;

const u: undefined = undefined;
const n: null = null;

const error: Error = new Error("我错了～");
const date: Date = new Date();
const r: RegExp = /[a-z]/;


// void类型不能赋值给其他类型 undefined 和 null 可以 void主要用来定义一个 无返回值的function

// Symbol、Document、HTMLElement、Event、NodeList、IArguments



